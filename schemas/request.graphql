###############
# Request
###############
type Request {
  accountId: ID!
  targetAccountId: ID!
  status: RequestStatus!
  account: Account!
  targetAccount: Account!
}

###############
# Enum
###############
enum RequestStatus {
  inProcess
  accept
  deny
  breakInProcess
  breakAccept
  breakDeny
}

###############
# Input
###############
input RequestInput {
  accountId: ID!
  targetAccountId: ID!
  status: RequestStatus
}
