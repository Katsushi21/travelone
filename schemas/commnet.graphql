###############
# Comment
###############
type Comment implements Node {
  id: ID!
  postId: ID!
  accountId: ID!
  body: String!
  createdAt: Time!
  updatedAt: Time!
  post: Post!
  account: Account!
}

###############
# Input
###############
input CommentInput {
  postId: ID!
  accountId: ID!
  body: String!
}
